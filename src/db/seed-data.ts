/**
 * The Product Owner's entities, re-exported from the copied model.
 *
 * The rows are authored in remy-sport-biz and copied verbatim by
 * `mise run domain:sync` — see scripts/domain-sync.ts for why a copy rather
 * than a generator. This file exists so the many importers do not each have to
 * know where the model sits.
 */
export { SEED_ENTITIES, SEED_RELATIONSHIPS } from "../domain/model/entities"
