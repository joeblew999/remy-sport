node_modules/
.wrangler/
dist/
.dev.vars
test-results/
playwright-report/
.playwright/
# Regenerated by `mise run shots`. In the working tree so they are openable in
# an editor, out of history so the repo does not store a PNG per run.
screenshots/
.gem/

# Compiled by Paraglide from messages/*.json — build output, regenerated by
# `mise run i18n:generate` and by the Vite plugin on every build.
src/web/paraglide/
