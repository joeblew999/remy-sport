version: "3"

tasks:
  setup:
    desc: Install deps and apply local migrations (idempotent)
    cmds:
      - task: install
      - task: cf:d1:migrations:apply

  install:
    desc: Install dependencies (idempotent via checksum)
    sources:
      - package.json
      - bun.lock
    generates:
      - node_modules/.package-lock.json
    cmd: bun install

  dev:
    desc: Start local dev server
    deps: [setup]
    cmd: bunx wrangler dev

  check:
    desc: Type check the project (idempotent via checksum)
    sources:
      - src/**/*.ts
      - tsconfig.json
    cmd: bunx tsc --noEmit

  test:
    desc: Run playwright tests locally
    deps: [setup]
    cmd: bunx playwright test

  test:deployed:
    desc: Run playwright tests against deployed worker
    cmd: bunx playwright test
    vars:
      WORKER_NAME:
        sh: grep '^name' wrangler.toml | head -1 | cut -d'"' -f2
      CF_SUBDOMAIN:
        sh: |
          TOKEN=$(grep oauth_token ~/Library/Preferences/.wrangler/config/default.toml 2>/dev/null | cut -d'"' -f2)
          ACCT=$(bunx wrangler whoami 2>/dev/null | grep -oE '[a-f0-9]{32}')
          curl -sf -H "Authorization: Bearer $TOKEN" "https://api.cloudflare.com/client/v4/accounts/$ACCT/workers/subdomain" | python3 -c "import sys,json; print(json.load(sys.stdin)['result']['subdomain'])"
      DEPLOYED_URL: 'https://{{.WORKER_NAME}}.{{.CF_SUBDOMAIN}}.workers.dev'
    env:
      BASE_URL: "{{.DEPLOYED_URL}}"

  test:ui:
    desc: Run playwright tests with UI
    cmd: bunx playwright test --ui

  deploy:
    desc: Build, deploy, migrate remote, and test deployed worker (idempotent)
    cmds:
      - task: setup
      - task: check
      - task: test
      - task: versions
      - task: cf:deploy
      - task: cf:d1:migrations:apply:remote
      - task: test:deployed

  versions:
    desc: Generate versions.json with deployment info (idempotent via checksum)
    sources:
      - package.json
      - .git/HEAD
      - .git/refs/heads/**/*
    generates:
      - versions.json
    cmds:
      - |
        APP_VERSION=$(node -p "require('./package.json').version")
        cat <<EOF > versions.json
        {
          "_generated": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
          "app": "$APP_VERSION",
          "git": {
            "commit": "$(git rev-parse --short HEAD)",
            "branch": "$(git branch --show-current)",
            "tag": "$(git describe --tags --always 2>/dev/null || echo 'none')"
          }
        }
        EOF

includes:
  cf:
    taskfile: ./taskfiles/Taskfile.cloudflare.yml
    optional: false
